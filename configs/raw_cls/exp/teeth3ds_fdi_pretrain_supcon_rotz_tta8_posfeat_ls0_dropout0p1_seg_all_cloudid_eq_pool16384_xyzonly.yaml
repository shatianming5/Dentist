defaults:
  - teeth3ds_fdi_pretrain_supcon_rotz_tta8_posfeat_ls0_dropout0p1_seg_all_cloudid_eq_xyzonly.yaml
  - ../data_v20_main4_seg_all_cloudid_eq_pool16384.yaml

exp:
  name: teeth3ds_fdi_pretrain_supcon_rotz_tta8_posfeat_ls0_dropout0p1_seg_all_cloudid_eq_pool16384_xyzonly

runtime:
  # Deterministic per-sample subsampling is easier to reason about without multi-worker RNG.
  num_workers: 0

