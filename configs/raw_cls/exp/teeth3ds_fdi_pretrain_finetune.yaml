defaults:
  - baseline.yaml
  - ../model_pointnet.yaml

exp:
  name: teeth3ds_fdi_pretrain_finetune

train:
  # Weights from `scripts/phase2_train_teeth3ds_fdi_cls.py` (FDI classification pretrain on Teeth3DS single-tooth).
  init_feat: runs/pretrain/teeth3ds_fdi_pointnet_seed1337/ckpt_best.pt
  freeze_feat_epochs: 0

