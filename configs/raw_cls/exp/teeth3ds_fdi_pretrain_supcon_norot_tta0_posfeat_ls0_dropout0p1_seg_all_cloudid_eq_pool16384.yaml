defaults:
  - teeth3ds_fdi_pretrain_supcon_norot_tta0_posfeat_ls0_dropout0p1.yaml
  - ../data_v20_main4_seg_all_cloudid_eq_pool16384.yaml

exp:
  name: teeth3ds_fdi_pretrain_supcon_norot_tta0_posfeat_ls0_dropout0p1_seg_all_cloudid_eq_pool16384

features:
  point_features: [xyz, cloud_id]

runtime:
  # Deterministic per-sample subsampling is easier to reason about without multi-worker RNG.
  num_workers: 0

