defaults:
  - teeth3ds_fdi_pretrain_supcon_rotz_tta8_posfeat_ls0_dropout0p1_seg_all_cloudid_eq_xyzonly.yaml
  - ../model_dgcnn.yaml

exp:
  name: dgcnn_supcon_rotz_tta8_posfeat_ls0_dropout0p1_seg_all_cloudid_eq_xyzonly_n1024

data:
  # DGCNN uses O(N^2) kNN; keep N modest for speed/memory.
  n_points: 1024

runtime:
  # Keep memory predictable (and avoid per-worker CUDA context overhead).
  num_workers: 0

train:
  # DGCNN cannot load PointNet init weights; train from scratch.
  init_feat: ""
  freeze_feat_epochs: 0
  batch_size: 32
