# Python caches
__pycache__/
*.pyc
*.pyo

# Local experiment queue (logs/results)
/.rd_queue/

# OS / editor
.DS_Store
.vscode/

# Large local datasets / artifacts (kept locally, not committed)
/data/teeth3ds/
/raw/
/converted/
/processed/

# Large run outputs (keep the short guide only)
/runs/*
!/runs/README.md

# Large model weights (kept locally, not committed)
/weights/

# Archives can be huge; keep only the small meta docs we generate
/archives/*
!/archives/_meta/
!/archives/_meta/**

# Meta subfolders that are not meant for publishing
/archives/_meta/tmp/
/archives/_meta/caches/
/archives/_meta/rd_queue/

# Internal "intraoral database" (PII / large) + rendered artifacts (local only)
/downloads/
/metadata/internal_db/
/outputs/
